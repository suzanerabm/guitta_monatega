---
interface Props {
  aboutPath: string;
  aboutLabel: string;
}

const { aboutPath, aboutLabel } = Astro.props;
---

<footer>
  <div class="footer-line"></div>
  <a href={aboutPath} class="footer-name">{aboutLabel}</a>
</footer>
